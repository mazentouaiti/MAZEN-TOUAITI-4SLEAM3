version: '3.8'
services:
  student-management:
    build: .
    ports:
      - "8080:8080"
    restart: unless-stopped
    environment:
      - SPRING_PROFILES_ACTIVE=docker
      - JAVA_OPTS=-Xmx512m -Xms256m
    # Add database if needed (uncomment and modify)
    # depends_on:
    #   - postgres-db
    # networks:
    #   - app-network

  # Uncomment and configure if you need a database
  # postgres-db:
  #   image: postgres:15-alpine
  #   environment:
  #     POSTGRES_DB: studentdb
  #     POSTGRES_USER: admin
  #     POSTGRES_PASSWORD: secret
  #   ports:
  #     - "5432:5432"
  #   volumes:
  #     - postgres-data:/var/lib/postgresql/data
  #   networks:
  #     - app-network

# volumes:
#   postgres-data:

# networks:
#   app-network:
#     driver: bridge
